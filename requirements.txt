# Requirements for Bulk Customer Import GUI
# Install with: pip install -r requirements.txt

# Core dependencies
requests>=2.25.0
urllib3>=1.26.0

# GUI dependencies (usually included with Python)
# tkinter is built-in with Python

# Build dependencies (for creating exe)
pyinstaller>=5.0.0
altgraph>=0.17.0

# Optional: For better GUI themes
# pillow>=8.0.0

# Development dependencies (optional)
# pytest>=6.0.0